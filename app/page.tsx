"use client"; import Link from "next/link"; import {useEffect,useMemo,useState} from "react";
type P={id:string;category?:string;serviceType?:string;name:string;description?:string;priceKobo:number}; type User={id:string;email:string;role:string};
export default function Home(){const[products,setProducts]=useState<P[]>([]);const[user,setUser]=useState<User|null>(null);const[selected,setSelected]=useState<P|null>(null);const[phone,setPhone]=useState("");const[amount,setAmount]=useState("");const[details,setDetails]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/products").then(r=>r.json()).then(setProducts);fetch("/api/auth/me").then(r=>r.json()).then(d=>setUser(d.user))},[]);const groups=useMemo(()=>{const m=new Map<string,P[]>();for(const p of products){const k=p.serviceType||p.category||"digital";m.set(k,[...(m.get(k)||[]),p])}return m},[products]);async function buy(){if(!selected||!user)return;setBusy(true);try{const type=selected.serviceType||selected.category||"digital";const payload:any={};if(type==="airtime"){if(!phone||!amount)throw new Error("Enter phone and amount");payload.amount=Number(amount);payload.mobile_number=phone.trim();payload.Ported_number=false;payload.airtime_type="VTU"}else if(type==="data"){if(!phone)throw new Error("Enter phone number");payload.mobile_number=phone.trim();payload.Ported_number=false}else if(type==="electricity"){const[a,b]=details.split("|");if(!a||!b||!amount)throw new Error("Enter meter|prepaid or meter|postpaid and amount");payload.amount=Number(amount);payload.billersCode=a;payload.variation_code=b}else if(type==="tv"){const[a,b]=details.split("|");if(!a||!b)throw new Error("Enter smartcard|subscription");payload.billersCode=a;payload.subscription_type=b}else if(type==="social_boost"){payload.link=details;payload.quantity=1}else if(type==="bulk_sms"||type==="bulksms"||type==="sms"){const[a,b,c]=details.split("|");if(!a||!b||!c)throw new Error("Use Sender|Recipients|Message");payload.sender=a;payload.recipient=b;payload.message=c}else payload.quantity=Number(details||1);const r=await fetch("/api/purchase",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({productId:selected.id,qty:1,purchaseData:payload})});const d=await r.json();if(!r.ok)throw new Error(d.error);setMsg(d.message||"Purchase submitted");setSelected(null)}catch(e){setMsg(e instanceof Error?e.message:"Purchase failed")}finally{setBusy(false)}}return <><section className="hero"><p className="badge">MultiKartX • JejeLaye powered</p><h1>Buy airtime, data and digital services from one wallet.</h1><p>Neon-backed wallet accounting with server-side JejeLaye fulfillment.</p><div className="row" style={{justifyContent:"flex-start",marginTop:22}}>{user?<Link className="btn" href="/wallet">Open wallet</Link>:<Link className="btn" href="/sign-in">Sign in to start</Link>}<Link className="btn secondary" href="/orders">Orders</Link></div></section>{msg&&<div className="alert success">{msg}</div>}{Array.from(groups.entries()).map(([cat,rows])=><section key={cat}><h2>{cat.replaceAll("_"," ")}</h2><div className="grid">{rows.map(p=><article className="card" key={p.id}><span className="badge">{p.serviceType||p.category||"Digital"}</span><h2>{p.name}</h2><p>{p.description}</p><div className="price">₦{(p.priceKobo/100).toLocaleString("en-NG",{minimumFractionDigits:2})}</div><button className="btn" disabled={!user} onClick={()=>{setSelected(p);setAmount(String(p.priceKobo/100));setDetails("");setPhone("")}}>Buy now</button></article>)}</div></section>)}{selected&&<div className="card" style={{position:"fixed",inset:"10% 5%",zIndex:20,overflow:"auto"}}><h2>{selected.name}</h2>{["airtime","data"].includes(selected.serviceType||"")&&<input placeholder="08012345678" value={phone} onChange={e=>setPhone(e.target.value)}/>} {["airtime","electricity"].includes(selected.serviceType||"")&&<input type="number" placeholder="Amount NGN" value={amount} onChange={e=>setAmount(e.target.value)}/>} {selected.serviceType==="electricity"&&<input placeholder="Meter|prepaid" value={details} onChange={e=>setDetails(e.target.value)}/>} {selected.serviceType==="tv"&&<input placeholder="Smartcard|renew" value={details} onChange={e=>setDetails(e.target.value)}/>} {["social_boost","bulk_sms","bulksms","sms"].includes(selected.serviceType||"")&&<input placeholder={selected.serviceType==="social_boost"?"Target URL":"Sender|Recipients|Message"} value={details} onChange={e=>setDetails(e.target.value)}/>} {["education","print_card"].includes(selected.serviceType||"")&&<input type="number" min="1" placeholder="Quantity" value={details} onChange={e=>setDetails(e.target.value)}/>}<div className="row"><button className="btn" disabled={busy} onClick={buy}>{busy?"Processing…":"Confirm purchase"}</button><button className="btn secondary" onClick={()=>setSelected(null)}>Cancel</button></div></div>}</>}