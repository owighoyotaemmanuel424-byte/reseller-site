"use client"; import {useState} from "react"; export default function Admin(){const[m,setM]=useState("");async function sync(){const r=await fetch("/api/admin/sync",{method:"POST"});const d=await r.json();setM(r.ok?"Synced "+d.synced+" JejeLaye services":d.error)}return <section className="hero"><p className="badge">Admin</p><h1>JejeLaye catalog</h1><p>Sync live provider services into the Neon product catalog.</p><button className="btn" onClick={sync}>Sync services</button>{m&&<div className="alert success">{m}</div>}</section>}