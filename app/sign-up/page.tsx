"use client"; import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation"; export default function SignUp(){const r=useRouter();const[e,setE]=useState("");const[p,setP]=useState("");const[m,setM]=useState("");async function submit(x:FormEvent){x.preventDefault();const res=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:e,password:p})});const d=await res.json();if(!res.ok)return setM(d.error||"Registration failed");r.push("/")}return <section className="card"><h1>Create account</h1><form onSubmit={submit}><input type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)} required/><input type="password" minLength={8} placeholder="Password (8+ characters)" value={p} onChange={x=>setP(x.target.value)} required/><button className="btn">Create account</button></form>{m&&<p>{m}</p>}</section>}