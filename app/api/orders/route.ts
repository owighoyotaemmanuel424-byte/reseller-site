import { NextResponse } from "next/server"; import { getCurrentUser } from "@/lib/auth"; import { sql } from "@/lib/db"; export async function GET(){const u=await getCurrentUser();if(!u)return NextResponse.json({error:"Authentication required"},{status:401});const rows=await sql`SELECT id,product_name AS "productName",qty,total_kobo AS "totalKobo",status,provider_order_id AS "providerOrderId",created_at AS "createdAt",product_details AS "productDetails" FROM orders WHERE user_id=${u.id} ORDER BY created_at DESC LIMIT 100`;return NextResponse.json(rows)} export async function PATCH(req:Request){const u=await getCurrentUser();if(!u)return NextResponse.json({error:"Authentication required"},{status:401});const b=await req.json();const rows=await sql`SELECT id,status,created_at FROM orders WHERE id=${b.orderId} AND user_id=${u.id}`;if(!rows[0])return NextResponse.json({error:"Order not found"},{status:404});if(rows[0].status!=="completed")return NextResponse.json({error:"Only completed orders can be reported"},{status:400});if(Date.now()-new Date(rows[0].created_at).getTime()>7200000)return NextResponse.json({error:"Reporting window expired"},{status:400});await sql`UPDATE orders SET status='reported',reported_at=now(),report_reason=${String(b.reason||"Customer reported an issue").slice(0,500)} WHERE id=${b.orderId}`;return NextResponse.json({ok:true})}}